# docker-image

Example Docker image project. The `docker.image` builtin runs through the
Docker Go SDK and Docker daemon API, not a shell command.

Run from this directory when a Docker daemon is available:

```bash
bu1ld build app_image
```
