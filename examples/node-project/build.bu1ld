workspace {
  name = "node-project"
}

plugin node {
  source = local
  id = "org.bu1ld.node"
  version = "0.1.3"
  path = "../../plugins/node/plugin.toml"
}

node {
  backend = "auto"
}

task build {
  deps = ["node.build"]
}
